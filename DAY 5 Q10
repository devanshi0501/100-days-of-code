#include <stdio.h>

int main(void)
{
    int sec, h, m, s;

    printf("Enter the time in seconds");
    scanf("%d", &sec);

    h = sec / 3600;
    sec = sec % 3600;

    m = sec / 60;
    s = sec % 60;

    printf("Changed format=%d:%d:%d\n", h, m, s);

    return 0;
}
